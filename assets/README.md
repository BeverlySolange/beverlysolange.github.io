# Profile image

Add Beverly's profile image here using either exact filename:

`beverly-felten-profile.png`

`beverly-felten-profile.jpg`

The landing page will automatically place it in the portrait frame, preferring
the PNG when both are present. Use PNG for an opaque background. Aim for around
1200 × 1500 pixels or larger.
